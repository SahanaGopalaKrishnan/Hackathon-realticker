# Hugging Face API token (recommended). If omitted, backend uses a built-in heuristic analysis.
HF_API_KEY=your_hf_api_token_here
# Model to use (Hugging Face model id). Default: google/flan-t5-large
HF_MODEL=google/flan-t5-large
